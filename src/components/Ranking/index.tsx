import React, { FC, useEffect } from 'react';
import { Link } from 'react-router-dom';

import './style.scss';

type RankingComponentPropType = {

};
import './style.scss';

const Ranking: FC<RankingComponentPropType> = (props) => {
  return (
      <div className="ranking-warp"></div>
  );
};

export default Ranking;